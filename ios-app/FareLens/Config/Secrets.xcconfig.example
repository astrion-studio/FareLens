// FareLens Secrets Configuration Template
// Copy this file to Secrets.xcconfig and fill in your actual values

// Supabase Configuration
// Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
// NOTE: Use https:/$/() syntax to prevent // from being treated as a comment
SUPABASE_URL = https:/$()/YOUR_PROJECT_ID.supabase.co
SUPABASE_PUBLISHABLE_KEY = YOUR_SUPABASE_PUBLISHABLE_KEY_HERE

// Cloudflare Workers API (deployed endpoint)
// Deploy the worker first: cd cloudflare-workers && npx wrangler deploy
// NOTE: Use https:/$/() syntax to prevent // from being treated as a comment
CLOUDFLARE_WORKER_URL = https:/$()/YOUR_WORKER_NAME.YOUR_SUBDOMAIN.workers.dev
