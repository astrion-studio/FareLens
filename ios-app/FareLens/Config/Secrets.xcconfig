// FareLens Secrets Configuration Template
//
// IMPORTANT: This file defines defaults, then includes Secrets.xcconfig.local
// - Defaults are defined first (safe for CI/CD)
// - Secrets.xcconfig.local overrides defaults (gitignored, for local dev)
// - Create Secrets.xcconfig.local with your real values for local development
//
// FORMAT REQUIREMENTS (enforced by ConfigValidator):
// - All URLs must be complete https:// URLs with a host
// - Format: https://your-subdomain.domain.com
// - Invalid: "https:" or "http://..." or "https://localhost"

// ==================== Default Values (CI/CD) ====================
// These are safe placeholder values used when .local file doesn't exist

// Supabase Configuration
// Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
// Example: SUPABASE_URL = https://yourproject.supabase.co
SUPABASE_URL = https://placeholder.supabase.co
SUPABASE_PUBLISHABLE_KEY = placeholder_supabase_publishable_key_here

// Cloudflare Workers API
// Example: CLOUDFLARE_WORKER_URL = https://your-worker.workers.dev
CLOUDFLARE_WORKER_URL = https://farelens-api.woodcut-rabbles5e.workers.dev

// ==================== Local Overrides ====================
// Include Secrets.xcconfig.local last so its values override defaults above
// The ? means don't error if file doesn't exist (safe for CI)
#include? "Secrets.xcconfig.local"
